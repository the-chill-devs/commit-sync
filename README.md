# CommitSync

A lightweight bot that synchronizes commits between repositories and branches.

## Features

- Show commits and changes made to repos in Discord channel
- CI / webhook friendly

## Requirements

- Node.js 16+ or a container runtime (Docker)
- A personal access token or bot token with repo permissions (push/pull)
- Network access to the source and target repositories
